# 🤖 وثيقة التوصيف لتكامل الذكاء الاصطناعي (AI Integration Blueprint)
**ملحق إضافي لـ AI Agent لتنفيذ الميزات (1, 2, 5, 6)**

**ملاحظة هامة للـ AI Agent:**
*(مزود الخدمة الخاص بالذكاء الاصطناعي وطريقة الاتصال به API Key / SDK موجودة في الملف المرفق الآخر. دورك هنا هو بناء المنطق البرمجي (Logic)، تعديلات قاعدة البيانات (Migrations)، وتحديث واجهات المستخدم (Frontend/Filament) لاستقبال وإرسال البيانات من وإلى الذكاء الاصطناعي).*

---

## 🛠️ أولاً: التعديلات المطلوبة على قاعدة البيانات (Database Migrations)
يجب إنشاء ملف Migration جديد لإضافة الحقول التي ستخزن نتائج الذكاء الاصطناعي في الجداول الموجودة:

```bash
php artisan make:migration add_ai_fields_to_tables
```

**محتوى الـ Migration:**
```php
public function up(): void
{
    // تعديلات جدول الشكاوى
    Schema::table('complaints', function (Blueprint $table) {
        $table->string('ai_priority')->nullable()->comment('high, medium, low');
        $table->text('ai_summary')->nullable();
    });

    // تعديلات جدول المرفقات (للتحقق من صحة المستندات)
    Schema::table('complaint_attachments', function (Blueprint $table) {
        $table->boolean('is_ai_verified')->nullable()->comment('true if AI confirms it is a valid document');
        $table->text('ai_ocr_text')->nullable()->comment('Text extracted from the image by AI');
    });
}
```
*(يجب إضافة هذه الحقول إلى مصفوفة `$fillable` في الموديلات الخاصة بها).*

---

## 🚀 ثانياً: خطة التنفيذ المفصلة لكل ميزة (Feature by Feature)

### 💡 الميزة 1: التصنيف التلقائي وتحديد أولوية الشكاوى (Smart Routing & Prioritization)
*   **الهدف:** تلخيص الشكاوى الطويلة آلياً وتحديد مدى خطورتها لتبسيط عمل الموظف.
*   **مكان التنفيذ:** (Backend - Laravel Observers أو داخل `ComplaintController::store`).
*   **آلية العمل (Workflow):**
    1. عند قيام المواطن بحفظ شكوى جديدة، يتم التقاط النص (`description`).
    2. يتم إرسال النص للـ AI بالاعتماد على الـ Prompt المخصص.
    3. يتم تحويل إجابة الـ AI (JSON) إلى مصفوفة، وتُحفظ في حقلي `ai_summary` و `ai_priority`.
*   **الـ Prompt المطلوب:**
    *"قم بتحليل هذه الشكوى: '{description}'. أعد لي استجابة بصيغة JSON فقط تحتوي على مفتاحين: 'summary' (تلخيص للمشكلة في سطر واحد)، و 'priority' (إما 'high' أو 'medium' أو 'low' بناءً على مدى خطورة المشكلة)."*
*   **في واجهة الموظف (Filament):** يتم عرض `ai_priority` كـ Badge ملون (أحمر للـ high، أصفر للـ medium، رمادي للـ low)، وعرض `ai_summary` كنص بارز فوق الوصف الأصلي.

---

### 💡 الميزة 2: المولد الآلي للردود الرسمية (Auto-Reply Drafter)
*   **الهدف:** تحويل الملاحظات المختصرة للموظف إلى رد رسمي يُرسل للمواطن.
*   **مكان التنفيذ:** (لوحة الموظف - Filament Panel).
*   **آلية العمل (Workflow):**
    1. داخل `ComplaintResource` أو `InquiryResource`، قم بإنشاء إجراء مخصص (Filament Action) باسم `Change Status & Reply`.
    2. الإجراء يفتح نافذة منبثقة (Modal) تحتوي على حقلين:
       * `status` (Select: completed, rejected).
       * `employee_quick_note` (Textarea).
    3. بجانب الحقل زر 🪄 **"توليد رد رسمي"**. عند النقر عليه، يُرسل النص القصير للـ AI.
    4. يعيد الـ AI نصاً رسمياً يتم وضعه في حقل إرسال النتيجة ليراجعه الموظف ويضغط "حفظ".
*   **الـ Prompt المطلوب:**
    *"كممثل رسمي لجهة حكومية، قم بتحويل هذه الملاحظة القصيرة: '{employee_quick_note}' إلى رسالة رسمية، مهذبة، ومختصرة لترسل للمواطن بخصوص معاملته. لا تضف أي معلومات غير موجودة في الملاحظة الأصلية."*

---

### 💡 الميزة 5: مصحح ومدقق النصوص الذكي (Complaint Enhancer)
*   **الهدف:** مساعدة المواطن على صياغة شكواه بأسلوب رسمي وواضح ومفهوم.
*   **مكان التنفيذ:** (واجهة المواطن - Frontend Blade & Alpine.js).
*   **آلية العمل (Workflow):**
    1. إنشاء مسار جديد (Route API) في `routes/web.php` مثلاً `/ai/enhance-text`.
    2. إنشاء تابع في `AiController` يستقبل النص من الـ Request، يرسله للـ AI، ويعيد النص المحسن.
    3. في صفحة `complaints.create`، وفي حقل إدخال الشكوى (`<textarea>`)، نستخدم **Alpine.js**.
    4. نضيف زراً: ✨ **"حسّن الصياغة بالذكاء الاصطناعي"**. عند الضغط عليه، يتم عمل طلب (Fetch/AJAX) للمسار السابق.
    5. يتم عرض (Loading spinner) أثناء الانتظار، وعند استلام الرد يتم استبدال النص الموجود في الـ Textarea بالنص المحسن فوراً.
*   **الـ Prompt المطلوب:**
    *"قم بتحسين وصياغة النص التالي ليكون شكوى رسمية موجهة لجهة حكومية. صحح الأخطاء الإملائية، واجعله واضحاً، مهذباً، ومباشراً. أعد النص المحسن فقط بدون أي إضافات: '{text}'."*

---

### 💡 الميزة 6: استخراج البيانات والتحقق من المرفقات (Vision AI Verification)
*   **الهدف:** فحص الصور المرفوعة من قبل المواطن (مثل الهوية أو الفواتير) للتأكد من أنها ليست صوراً عشوائية واستخراج النص منها لتسهيل البحث.
*   **مكان التنفيذ:** (Backend - عند رفع المرفقات).
*   **آلية العمل (Workflow):**
    1. عند رفع ملف إلى `ComplaintAttachment` أو `Inquiry` (إذا كان نوع الملف صورة: jpg, png)، يتم تحويل مسار الصورة إلى Base64 أو تمريرها لمزود الذكاء الاصطناعي.
    2. يُطلب من نموذج الرؤية (Vision AI) فحص الصورة.
    3. إذا تحقق الـ AI من أنها وثيقة، يتم تخزين النتيجة في `is_ai_verified = true` والنص المستخرج في `ai_ocr_text`.
    4. في لوحة الإدارة (Filament)، يظهر للموظف علامة (✅ تم التحقق آلياً) بجانب المرفق، مما يوفر عليه عناء التدقيق اليدوي.
*   **الـ Prompt المطلوب (مرفق مع الصورة):**
    *"هل هذه الصورة عبارة عن وثيقة رسمية، هوية شخصية، أو فاتورة؟ إذا نعم، أعد JSON يحتوي على 'is_valid': true و 'extracted_text': 'النص الموجود في الصورة'. إذا كانت صورة عشوائية (مثلاً سيلفي أو منظر طبيعي)، أعد 'is_valid': false."*

---

**أيها الـ AI Agent:**
تم تسليمك المخطط وقواعد العمل. يرجى مراجعة الملف الآخر المرفق للحصول على معلومات (API integration)، وابدأ فوراً بإنشاء التعديلات على الـ Migrations والـ Controllers وتطبيق ميزات Alpine.js في واجهات الواجهة الأمامية!